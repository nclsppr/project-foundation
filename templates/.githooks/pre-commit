#!/usr/bin/env bash
set -euo pipefail

PROJECT_ROOT="$(git rev-parse --show-toplevel)"

if [[ -x "${PROJECT_ROOT}/.githooks/pre-commit.local" ]]; then
  "${PROJECT_ROOT}/.githooks/pre-commit.local" "$@"
fi

exec "${PROJECT_ROOT}/scripts/verify.sh"
